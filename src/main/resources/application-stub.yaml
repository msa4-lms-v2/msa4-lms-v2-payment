# "stub" 프로파일 - Academic 서비스 없이 단독 기동할 때 사용한다 (MY-PLAN_payment.md 4-1절).
# AcademicStubClient(@Profile("stub"))가 AcademicHttpClient 대신 활성화된다.
# 실행: SPRING_PROFILES_ACTIVE=stub ./gradlew bootRun
logging:
  level:
    com.msa4lmsv2payment.global.client: info
